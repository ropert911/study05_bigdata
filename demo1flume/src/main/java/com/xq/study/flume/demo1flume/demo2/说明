启动命令：
flume-ng agent --conf conf --conf-file demo.properties --name a1

使用：
    telnet 127.0.0.1 44444 进行输入

本示例：
    source：netcat  是flume内置的source的名称，配置里用的别名。不是类路径名称
    channel：memory 是内存方式的channel
    sink：logger 